# MACHINE-GENERATED by bench/public_claims.py — do not hand-edit.
# known_truth is the verdict OBSERVED by running the C3 checker on the mutated copy.
schema_version: 1
status: benchmark-ready
mutations:
- id: "mut-naturalsize-param-rename"
  claim_id: "naturalsize-signature"
  file: "src/humanize/filesize.py"
  op: "replace"
  from: "    gnu: bool = False,"
  to: "    use_gnu: bool = False,"
  known_truth: "BROKEN"
  rationale: "machine-derived: py-signature FAILs iff the def parameter list (names/order/kind) changes; a comment/body-only edit leaves it PASS. Label = observed verdict. Observed FAIL on the mutated copy at the frozen SHA."
- id: "mut-ordinal-comment-only"
  claim_id: "ordinal-signature"
  file: "src/humanize/number.py"
  op: "replace"
  from: "    if value % 100 in (11, 12, 13):  # special case"
  to: "    if value % 100 in (11, 12, 13):  # teens take 'th' (11th/12th/13th)"
  known_truth: "TRUSTED"
  rationale: "machine-derived: py-signature FAILs iff the def parameter list (names/order/kind) changes; a comment/body-only edit leaves it PASS. Label = observed verdict. Observed PASS on the mutated copy at the frozen SHA."
- id: "mut-gnu-suffix-drop-q"
  claim_id: "gnu-suffix-code"
  file: "src/humanize/filesize.py"
  op: "replace"
  from: "\"KMGTPEZYRQ\""
  to: "\"KMGTPEZYR\""
  known_truth: "BROKEN"
  rationale: "machine-derived: code: matches an exact token in comment/docstring-stripped source; removing/altering it FAILs, a comment-only edit leaves it PASS. Label = observed verdict. Observed FAIL on the mutated copy at the frozen SHA."
- id: "mut-project-name-change"
  claim_id: "project-name-config"
  file: "pyproject.toml"
  op: "replace"
  from: "name = \"humanize\""
  to: "name = \"humanize-mut\""
  known_truth: "BROKEN"
  rationale: "machine-derived: config-value compares the value at the dotted key by value AND type; any change FAILs. Label = observed verdict. Observed FAIL on the mutated copy at the frozen SHA."
